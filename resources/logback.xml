<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- 로그 경로 (Working Directory 기준) -->
  <property name="LOG_DIR" value="logs"/>

  <!-- 콘솔(원래 있던 것 유지) -->
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss} %-5level [%thread] %logger - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- 운영로그 전용 파일 appender -->
  <appender name="AO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/ao/app-ops.log</file>
    
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
        <fileNamePattern>${LOG_DIR}/ao/backup/app-ops-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
        <maxFileSize>10MB</maxFileSize>
      <maxHistory>14</maxHistory> </rollingPolicy>

    <encoder>
        <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [%X{traceId}] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>



  <!-- 거래로그 전용 파일 appender -->
  <appender name="TXN_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/txn/txn.log</file>

    <!-- 일 단위 + 용량 단위 롤링 + gzip -->
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/txn/archive/txn.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
      <maxFileSize>10MB</maxFileSize>
      <maxHistory>14</maxHistory>
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>

    <encoder>
      <!-- 토이니까 key=value 스타일로 충분 -->
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %msg%n</pattern>
    </encoder>
  </appender>

  <logger name="ApplicationLogger" level="INFO" additivity="false">
    <appender-ref ref="AO_FILE" />
    <appender-ref ref="STDOUT" />
  </logger>

  <!-- TXN_LOGGER로 찍힌 로그만 txn.log로 분리 -->
  <logger name="TXN_LOGGER" level="INFO" additivity="false">
    <appender-ref ref="TXN_FILE" />
  </logger>

  <!-- 운영 로그는 다른 팀원이 할 거라 root는 일단 콘솔만 -->
  <root level="INFO">
    <appender-ref ref="STDOUT" />
  </root>

</configuration>
